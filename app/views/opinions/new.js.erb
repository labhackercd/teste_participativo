var holder = $("#<%= params[:related_opinion_id] %>")



var button = $("#<%= "card-action-#{params[:related_opinion_id]}" %>");

button.hide();
holder.after("<%= escape_javascript(render 'opinions/new')%>")
